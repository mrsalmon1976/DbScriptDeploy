<Window x:Class="DbScriptDeploy.UI.Controls.ScriptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ScriptDialog" Height="400" Width="600">
    <Grid>
        <DockPanel LastChildFill="True">
            <DockPanel DockPanel.Dock="Top" Margin="3,5,5,5">
                <Label Content="Name:" Margin="0,0,20,0" />
                <TextBox Name="txtName" />
            </DockPanel>
            <DockPanel DockPanel.Dock="Bottom" LastChildFill="False">
                <Button Name="btnCancel" Content="Cancel" DockPanel.Dock="Right" IsCancel="True" IsDefault="True" Margin="5" Padding="15,0,15,0" />
                <Button Name="btnParse" Content="Parse" DockPanel.Dock="Right" Click="btnParse_Click" Margin="5" Padding="15,0,15,0" />
                <Button Name="btnSave" Content="Save" DockPanel.Dock="Right" Click="btnSave_Click" Margin="5" Padding="15,0,15,0" />
            </DockPanel>
            <WindowsFormsHost Name="scintillaHost"></WindowsFormsHost>

        </DockPanel>
        
    </Grid>
</Window>
