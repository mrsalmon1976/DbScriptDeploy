{
  "AppSettings": {
    "Port": 5432,
    // set the email provider - if "None" is selected then password resets will need to be done 
    // using a generated link (not particularly secure). 
    // Options are: None, SendGrid
    "EmailProvider": "None"
  },
  "ConnectionStrings": {
    "DefaultConnection": "Filename=./db/DbScriptDeploy.db"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
